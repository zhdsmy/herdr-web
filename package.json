{
  "name": "herdr-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "test:web": "npm run test --prefix web",
    "lint:web": "npm run lint --prefix web",
    "check:web": "npm run lint:web && npm run test:web && npm run build:web",
    "deploy:web": "scripts/deploy-web.sh",
    "ship:web": "npm run check:web && npm run deploy:web",
    "vendor:check": "scripts/check-vendor.sh",
    "bridge:fmt": "cargo fmt --manifest-path bridge/Cargo.toml --check && cargo fmt --manifest-path vendor/herdr-compat/Cargo.toml --check",
    "bridge:build": "cargo build --manifest-path bridge/Cargo.toml --bin herdr-web-bridge",
    "bridge:test": "cargo test --manifest-path vendor/herdr-compat/Cargo.toml && cargo test --manifest-path bridge/Cargo.toml --bin herdr-web-bridge",
    "android:sync": "npm run build:web && cap sync android",
    "android:build:debug": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:open": "cap open android",
    "package:tarball": "scripts/package-tarball.sh",
    "build": "npm run build:web && npm run bridge:build",
    "test": "npm run test:web && npm run bridge:test",
    "lint": "npm run lint:web && npm run bridge:fmt",
    "check": "npm run vendor:check && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/keyboard": "^8.0.5",
    "@capacitor/preferences": "^8.0.1"
  }
}
